# Docker 环境配置
server:
  port: 8093

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/short_url?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
  data:
    redis:
      host: redis
      port: 6379
      password:

short-url:
  domain: http://localhost:8080
